export type InputDateType = {
    id: string
    name: string
    type: string
    key: string
}

export type FormValuesType = {
    email: string
    password: string
    password_confirm?: string
    rememberMe?: boolean
}